import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { RefeitorioPage } from './refeitorio';

@NgModule({
  declarations: [
    RefeitorioPage,
  ],
  imports: [
    IonicPageModule.forChild(RefeitorioPage),
  ],
})
export class RefeitorioPageModule {}
